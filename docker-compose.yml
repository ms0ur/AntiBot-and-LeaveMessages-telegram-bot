services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - DEBUG=${DEBUG:-false}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command: ["python", "bot.py"]
