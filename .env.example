# Telegram bot token provided by BotFather
BOT_TOKEN=your_bot_token_here

# Path to the SQLite database file (will be created if missing)
DATABASE_PATH=data/bot_data.sqlite

# Optional default parse mode for outgoing messages (e.g. HTML, MARKDOWN_V2)
PARSE_MODE=HTML

DEBUG=false